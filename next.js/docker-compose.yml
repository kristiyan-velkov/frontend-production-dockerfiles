services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile.standalone
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    restart: unless-stopped
